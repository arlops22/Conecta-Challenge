import React from 'react';

import { StyledButton } from './styles';

export default function Button({ darkTheme }) {
    return (
        <StyledButton darkTheme={darkTheme}>
            Aqui um CTA
        </StyledButton>
                )
}
